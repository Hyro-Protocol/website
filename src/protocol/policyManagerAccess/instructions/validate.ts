/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import { POLICY_MANAGER_ACCESS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getValidateOperationDecoder,
  getValidateOperationEncoder,
  type ValidateOperation,
  type ValidateOperationArgs,
} from '../types';

export const VALIDATE_DISCRIMINATOR = new Uint8Array([
  60, 252, 90, 66, 246, 253, 232, 139,
]);

export function getValidateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VALIDATE_DISCRIMINATOR);
}

export type ValidateInstruction<
  TProgram extends string = typeof POLICY_MANAGER_ACCESS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type ValidateInstructionData = {
  discriminator: ReadonlyUint8Array;
  operation: ValidateOperation;
};

export type ValidateInstructionDataArgs = { operation: ValidateOperationArgs };

export function getValidateInstructionDataEncoder(): FixedSizeEncoder<ValidateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['operation', getValidateOperationEncoder()],
    ]),
    (value) => ({ ...value, discriminator: VALIDATE_DISCRIMINATOR })
  );
}

export function getValidateInstructionDataDecoder(): FixedSizeDecoder<ValidateInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['operation', getValidateOperationDecoder()],
  ]);
}

export function getValidateInstructionDataCodec(): FixedSizeCodec<
  ValidateInstructionDataArgs,
  ValidateInstructionData
> {
  return combineCodec(
    getValidateInstructionDataEncoder(),
    getValidateInstructionDataDecoder()
  );
}

export type ValidateInput<TAccountVault extends string = string> = {
  vault: Address<TAccountVault>;
  operation: ValidateInstructionDataArgs['operation'];
};

export function getValidateInstruction<
  TAccountVault extends string,
  TProgramAddress extends
    Address = typeof POLICY_MANAGER_ACCESS_PROGRAM_ADDRESS,
>(
  input: ValidateInput<TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): ValidateInstruction<TProgramAddress, TAccountVault> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_MANAGER_ACCESS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.vault)],
    data: getValidateInstructionDataEncoder().encode(
      args as ValidateInstructionDataArgs
    ),
    programAddress,
  } as ValidateInstruction<TProgramAddress, TAccountVault>);
}

export type ParsedValidateInstruction<
  TProgram extends string = typeof POLICY_MANAGER_ACCESS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
  };
  data: ValidateInstructionData;
};

export function parseValidateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedValidateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vault: getNextAccount() },
    data: getValidateInstructionDataDecoder().decode(instruction.data),
  };
}
