/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { REVENUE_GENERATOR_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const GENERATE_REVENUE_DISCRIMINATOR = new Uint8Array([
  251, 232, 131, 80, 58, 114, 70, 83,
]);

export function getGenerateRevenueDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GENERATE_REVENUE_DISCRIMINATOR,
  );
}

export type GenerateRevenueInstruction<
  TProgram extends string = typeof REVENUE_GENERATOR_PROGRAM_ADDRESS,
  TAccountGenerator extends string | AccountMeta<string> = string,
  TAccountUnderlyingMint extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGenerator extends string
        ? WritableAccount<TAccountGenerator>
        : TAccountGenerator,
      TAccountUnderlyingMint extends string
        ? WritableAccount<TAccountUnderlyingMint>
        : TAccountUnderlyingMint,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GenerateRevenueInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GenerateRevenueInstructionDataArgs = {};

export function getGenerateRevenueInstructionDataEncoder(): FixedSizeEncoder<GenerateRevenueInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GENERATE_REVENUE_DISCRIMINATOR }),
  );
}

export function getGenerateRevenueInstructionDataDecoder(): FixedSizeDecoder<GenerateRevenueInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGenerateRevenueInstructionDataCodec(): FixedSizeCodec<
  GenerateRevenueInstructionDataArgs,
  GenerateRevenueInstructionData
> {
  return combineCodec(
    getGenerateRevenueInstructionDataEncoder(),
    getGenerateRevenueInstructionDataDecoder(),
  );
}

export type GenerateRevenueAsyncInput<
  TAccountGenerator extends string = string,
  TAccountUnderlyingMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  generator?: Address<TAccountGenerator>;
  underlyingMint: Address<TAccountUnderlyingMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getGenerateRevenueInstructionAsync<
  TAccountGenerator extends string,
  TAccountUnderlyingMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof REVENUE_GENERATOR_PROGRAM_ADDRESS,
>(
  input: GenerateRevenueAsyncInput<
    TAccountGenerator,
    TAccountUnderlyingMint,
    TAccountVaultTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  GenerateRevenueInstruction<
    TProgramAddress,
    TAccountGenerator,
    TAccountUnderlyingMint,
    TAccountVaultTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REVENUE_GENERATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    generator: { value: input.generator ?? null, isWritable: true },
    underlyingMint: { value: input.underlyingMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.generator.value) {
    accounts.generator.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 101, 110, 101, 114, 97, 116, 111, 114]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.generator),
      getAccountMeta(accounts.underlyingMint),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getGenerateRevenueInstructionDataEncoder().encode({}),
    programAddress,
  } as GenerateRevenueInstruction<
    TProgramAddress,
    TAccountGenerator,
    TAccountUnderlyingMint,
    TAccountVaultTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type GenerateRevenueInput<
  TAccountGenerator extends string = string,
  TAccountUnderlyingMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  generator: Address<TAccountGenerator>;
  underlyingMint: Address<TAccountUnderlyingMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getGenerateRevenueInstruction<
  TAccountGenerator extends string,
  TAccountUnderlyingMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof REVENUE_GENERATOR_PROGRAM_ADDRESS,
>(
  input: GenerateRevenueInput<
    TAccountGenerator,
    TAccountUnderlyingMint,
    TAccountVaultTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): GenerateRevenueInstruction<
  TProgramAddress,
  TAccountGenerator,
  TAccountUnderlyingMint,
  TAccountVaultTokenAccount,
  TAccountAuthority,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REVENUE_GENERATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    generator: { value: input.generator ?? null, isWritable: true },
    underlyingMint: { value: input.underlyingMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.generator),
      getAccountMeta(accounts.underlyingMint),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getGenerateRevenueInstructionDataEncoder().encode({}),
    programAddress,
  } as GenerateRevenueInstruction<
    TProgramAddress,
    TAccountGenerator,
    TAccountUnderlyingMint,
    TAccountVaultTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type ParsedGenerateRevenueInstruction<
  TProgram extends string = typeof REVENUE_GENERATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    generator: TAccountMetas[0];
    underlyingMint: TAccountMetas[1];
    vaultTokenAccount: TAccountMetas[2];
    authority: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: GenerateRevenueInstructionData;
};

export function parseGenerateRevenueInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGenerateRevenueInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      generator: getNextAccount(),
      underlyingMint: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      authority: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getGenerateRevenueInstructionDataDecoder().decode(instruction.data),
  };
}
