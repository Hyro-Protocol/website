/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/** Time-based fee entry for a specific fee type */
export type TimeBasedEntry = {
  /** Whether this time-based fee is enabled */
  enabled: boolean;
  /** Fee calculation mode (0 = FixedAmount, 1 = AumPercentage) */
  mode: number;
  /**
   * For FixedAmount: the fixed amount per period
   * For AumPercentage: basis points per period (100 = 1%)
   */
  value: bigint;
  /** Time period for fee accrual (0=Daily, 1=Weekly, 2=Monthly, 3=Quarterly, 4=Annually) */
  period: number;
};

export type TimeBasedEntryArgs = {
  /** Whether this time-based fee is enabled */
  enabled: boolean;
  /** Fee calculation mode (0 = FixedAmount, 1 = AumPercentage) */
  mode: number;
  /**
   * For FixedAmount: the fixed amount per period
   * For AumPercentage: basis points per period (100 = 1%)
   */
  value: number | bigint;
  /** Time period for fee accrual (0=Daily, 1=Weekly, 2=Monthly, 3=Quarterly, 4=Annually) */
  period: number;
};

export function getTimeBasedEntryEncoder(): FixedSizeEncoder<TimeBasedEntryArgs> {
  return getStructEncoder([
    ["enabled", getBooleanEncoder()],
    ["mode", getU8Encoder()],
    ["value", getU64Encoder()],
    ["period", getU8Encoder()],
  ]);
}

export function getTimeBasedEntryDecoder(): FixedSizeDecoder<TimeBasedEntry> {
  return getStructDecoder([
    ["enabled", getBooleanDecoder()],
    ["mode", getU8Decoder()],
    ["value", getU64Decoder()],
    ["period", getU8Decoder()],
  ]);
}

export function getTimeBasedEntryCodec(): FixedSizeCodec<
  TimeBasedEntryArgs,
  TimeBasedEntry
> {
  return combineCodec(getTimeBasedEntryEncoder(), getTimeBasedEntryDecoder());
}
