/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';
import {
  getTimeBasedEntryDecoder,
  getTimeBasedEntryEncoder,
  type TimeBasedEntry,
  type TimeBasedEntryArgs,
} from '.';

/** Time-based fee configuration for all fee types */
export type TimeBasedConfig = {
  /** LP time-based fee */
  lp: TimeBasedEntry;
  /** Manager time-based fee (e.g., management company salary) */
  manager: TimeBasedEntry;
  /** Protocol time-based fee */
  protocol: TimeBasedEntry;
  /** Performance time-based fee */
  performance: TimeBasedEntry;
};

export type TimeBasedConfigArgs = {
  /** LP time-based fee */
  lp: TimeBasedEntryArgs;
  /** Manager time-based fee (e.g., management company salary) */
  manager: TimeBasedEntryArgs;
  /** Protocol time-based fee */
  protocol: TimeBasedEntryArgs;
  /** Performance time-based fee */
  performance: TimeBasedEntryArgs;
};

export function getTimeBasedConfigEncoder(): FixedSizeEncoder<TimeBasedConfigArgs> {
  return getStructEncoder([
    ['lp', getTimeBasedEntryEncoder()],
    ['manager', getTimeBasedEntryEncoder()],
    ['protocol', getTimeBasedEntryEncoder()],
    ['performance', getTimeBasedEntryEncoder()],
  ]);
}

export function getTimeBasedConfigDecoder(): FixedSizeDecoder<TimeBasedConfig> {
  return getStructDecoder([
    ['lp', getTimeBasedEntryDecoder()],
    ['manager', getTimeBasedEntryDecoder()],
    ['protocol', getTimeBasedEntryDecoder()],
    ['performance', getTimeBasedEntryDecoder()],
  ]);
}

export function getTimeBasedConfigCodec(): FixedSizeCodec<
  TimeBasedConfigArgs,
  TimeBasedConfig
> {
  return combineCodec(getTimeBasedConfigEncoder(), getTimeBasedConfigDecoder());
}
