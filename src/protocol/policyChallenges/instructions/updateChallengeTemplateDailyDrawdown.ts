/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { POLICY_CHALLENGES_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getPercentDecoder,
  getPercentEncoder,
  type Percent,
  type PercentArgs,
} from "../types";

export const UPDATE_CHALLENGE_TEMPLATE_DAILY_DRAWDOWN_DISCRIMINATOR =
  new Uint8Array([100, 174, 225, 136, 76, 206, 146, 63]);

export function getUpdateChallengeTemplateDailyDrawdownDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CHALLENGE_TEMPLATE_DAILY_DRAWDOWN_DISCRIMINATOR,
  );
}

export type UpdateChallengeTemplateDailyDrawdownInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountChallengeTemplateAccount extends string | AccountMeta<string> =
    string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallengeTemplateAccount extends string
        ? WritableAccount<TAccountChallengeTemplateAccount>
        : TAccountChallengeTemplateAccount,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateChallengeTemplateDailyDrawdownInstructionData = {
  discriminator: ReadonlyUint8Array;
  dailyDrawdown: Percent;
};

export type UpdateChallengeTemplateDailyDrawdownInstructionDataArgs = {
  dailyDrawdown: PercentArgs;
};

export function getUpdateChallengeTemplateDailyDrawdownInstructionDataEncoder(): FixedSizeEncoder<UpdateChallengeTemplateDailyDrawdownInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["dailyDrawdown", getPercentEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_CHALLENGE_TEMPLATE_DAILY_DRAWDOWN_DISCRIMINATOR,
    }),
  );
}

export function getUpdateChallengeTemplateDailyDrawdownInstructionDataDecoder(): FixedSizeDecoder<UpdateChallengeTemplateDailyDrawdownInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dailyDrawdown", getPercentDecoder()],
  ]);
}

export function getUpdateChallengeTemplateDailyDrawdownInstructionDataCodec(): FixedSizeCodec<
  UpdateChallengeTemplateDailyDrawdownInstructionDataArgs,
  UpdateChallengeTemplateDailyDrawdownInstructionData
> {
  return combineCodec(
    getUpdateChallengeTemplateDailyDrawdownInstructionDataEncoder(),
    getUpdateChallengeTemplateDailyDrawdownInstructionDataDecoder(),
  );
}

export type UpdateChallengeTemplateDailyDrawdownInput<
  TAccountChallengeTemplateAccount extends string = string,
  TAccountSigner extends string = string,
> = {
  challengeTemplateAccount: Address<TAccountChallengeTemplateAccount>;
  signer: TransactionSigner<TAccountSigner>;
  dailyDrawdown: UpdateChallengeTemplateDailyDrawdownInstructionDataArgs["dailyDrawdown"];
};

export function getUpdateChallengeTemplateDailyDrawdownInstruction<
  TAccountChallengeTemplateAccount extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
>(
  input: UpdateChallengeTemplateDailyDrawdownInput<
    TAccountChallengeTemplateAccount,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateChallengeTemplateDailyDrawdownInstruction<
  TProgramAddress,
  TAccountChallengeTemplateAccount,
  TAccountSigner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_CHALLENGES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeTemplateAccount: {
      value: input.challengeTemplateAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.challengeTemplateAccount),
      getAccountMeta(accounts.signer),
    ],
    data: getUpdateChallengeTemplateDailyDrawdownInstructionDataEncoder().encode(
      args as UpdateChallengeTemplateDailyDrawdownInstructionDataArgs,
    ),
    programAddress,
  } as UpdateChallengeTemplateDailyDrawdownInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountSigner
  >);
}

export type ParsedUpdateChallengeTemplateDailyDrawdownInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challengeTemplateAccount: TAccountMetas[0];
    signer: TAccountMetas[1];
  };
  data: UpdateChallengeTemplateDailyDrawdownInstructionData;
};

export function parseUpdateChallengeTemplateDailyDrawdownInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateChallengeTemplateDailyDrawdownInstruction<
  TProgram,
  TAccountMetas
> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challengeTemplateAccount: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getUpdateChallengeTemplateDailyDrawdownInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
