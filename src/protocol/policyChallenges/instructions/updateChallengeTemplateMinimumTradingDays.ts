/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { POLICY_CHALLENGES_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getSmallScalarDecoder,
  getSmallScalarEncoder,
  type SmallScalar,
  type SmallScalarArgs,
} from '../types';

export const UPDATE_CHALLENGE_TEMPLATE_MINIMUM_TRADING_DAYS_DISCRIMINATOR =
  new Uint8Array([67, 107, 22, 76, 9, 91, 83, 8]);

export function getUpdateChallengeTemplateMinimumTradingDaysDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CHALLENGE_TEMPLATE_MINIMUM_TRADING_DAYS_DISCRIMINATOR
  );
}

export type UpdateChallengeTemplateMinimumTradingDaysInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountChallengeTemplateAccount extends
    | string
    | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallengeTemplateAccount extends string
        ? WritableAccount<TAccountChallengeTemplateAccount>
        : TAccountChallengeTemplateAccount,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateChallengeTemplateMinimumTradingDaysInstructionData = {
  discriminator: ReadonlyUint8Array;
  minimumTradingDays: SmallScalar;
};

export type UpdateChallengeTemplateMinimumTradingDaysInstructionDataArgs = {
  minimumTradingDays: SmallScalarArgs;
};

export function getUpdateChallengeTemplateMinimumTradingDaysInstructionDataEncoder(): FixedSizeEncoder<UpdateChallengeTemplateMinimumTradingDaysInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['minimumTradingDays', getSmallScalarEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator:
        UPDATE_CHALLENGE_TEMPLATE_MINIMUM_TRADING_DAYS_DISCRIMINATOR,
    })
  );
}

export function getUpdateChallengeTemplateMinimumTradingDaysInstructionDataDecoder(): FixedSizeDecoder<UpdateChallengeTemplateMinimumTradingDaysInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['minimumTradingDays', getSmallScalarDecoder()],
  ]);
}

export function getUpdateChallengeTemplateMinimumTradingDaysInstructionDataCodec(): FixedSizeCodec<
  UpdateChallengeTemplateMinimumTradingDaysInstructionDataArgs,
  UpdateChallengeTemplateMinimumTradingDaysInstructionData
> {
  return combineCodec(
    getUpdateChallengeTemplateMinimumTradingDaysInstructionDataEncoder(),
    getUpdateChallengeTemplateMinimumTradingDaysInstructionDataDecoder()
  );
}

export type UpdateChallengeTemplateMinimumTradingDaysInput<
  TAccountChallengeTemplateAccount extends string = string,
  TAccountSigner extends string = string,
> = {
  challengeTemplateAccount: Address<TAccountChallengeTemplateAccount>;
  signer: TransactionSigner<TAccountSigner>;
  minimumTradingDays: UpdateChallengeTemplateMinimumTradingDaysInstructionDataArgs['minimumTradingDays'];
};

export function getUpdateChallengeTemplateMinimumTradingDaysInstruction<
  TAccountChallengeTemplateAccount extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
>(
  input: UpdateChallengeTemplateMinimumTradingDaysInput<
    TAccountChallengeTemplateAccount,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateChallengeTemplateMinimumTradingDaysInstruction<
  TProgramAddress,
  TAccountChallengeTemplateAccount,
  TAccountSigner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_CHALLENGES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeTemplateAccount: {
      value: input.challengeTemplateAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.challengeTemplateAccount),
      getAccountMeta(accounts.signer),
    ],
    data: getUpdateChallengeTemplateMinimumTradingDaysInstructionDataEncoder().encode(
      args as UpdateChallengeTemplateMinimumTradingDaysInstructionDataArgs
    ),
    programAddress,
  } as UpdateChallengeTemplateMinimumTradingDaysInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountSigner
  >);
}

export type ParsedUpdateChallengeTemplateMinimumTradingDaysInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challengeTemplateAccount: TAccountMetas[0];
    signer: TAccountMetas[1];
  };
  data: UpdateChallengeTemplateMinimumTradingDaysInstructionData;
};

export function parseUpdateChallengeTemplateMinimumTradingDaysInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateChallengeTemplateMinimumTradingDaysInstruction<
  TProgram,
  TAccountMetas
> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challengeTemplateAccount: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getUpdateChallengeTemplateMinimumTradingDaysInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
