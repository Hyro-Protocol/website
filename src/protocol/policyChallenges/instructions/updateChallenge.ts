/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { POLICY_CHALLENGES_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getChallengeUpdateDtoDecoder,
  getChallengeUpdateDtoEncoder,
  type ChallengeUpdateDto,
  type ChallengeUpdateDtoArgs,
} from '../types';

export const UPDATE_CHALLENGE_DISCRIMINATOR = new Uint8Array([
  189, 212, 76, 181, 2, 202, 238, 16,
]);

export function getUpdateChallengeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CHALLENGE_DISCRIMINATOR
  );
}

export type UpdateChallengeInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountChallengeTemplateAccount extends
    | string
    | AccountMeta<string> = string,
  TAccountChallengeAccount extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallengeTemplateAccount extends string
        ? ReadonlyAccount<TAccountChallengeTemplateAccount>
        : TAccountChallengeTemplateAccount,
      TAccountChallengeAccount extends string
        ? WritableAccount<TAccountChallengeAccount>
        : TAccountChallengeAccount,
      TAccountSender extends string
        ? WritableSignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateChallengeInstructionData = {
  discriminator: ReadonlyUint8Array;
  dto: ChallengeUpdateDto;
};

export type UpdateChallengeInstructionDataArgs = {
  dto: ChallengeUpdateDtoArgs;
};

export function getUpdateChallengeInstructionDataEncoder(): Encoder<UpdateChallengeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['dto', getChallengeUpdateDtoEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_CHALLENGE_DISCRIMINATOR })
  );
}

export function getUpdateChallengeInstructionDataDecoder(): Decoder<UpdateChallengeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['dto', getChallengeUpdateDtoDecoder()],
  ]);
}

export function getUpdateChallengeInstructionDataCodec(): Codec<
  UpdateChallengeInstructionDataArgs,
  UpdateChallengeInstructionData
> {
  return combineCodec(
    getUpdateChallengeInstructionDataEncoder(),
    getUpdateChallengeInstructionDataDecoder()
  );
}

export type UpdateChallengeInput<
  TAccountChallengeTemplateAccount extends string = string,
  TAccountChallengeAccount extends string = string,
  TAccountSender extends string = string,
> = {
  challengeTemplateAccount: Address<TAccountChallengeTemplateAccount>;
  challengeAccount: Address<TAccountChallengeAccount>;
  sender: TransactionSigner<TAccountSender>;
  dto: UpdateChallengeInstructionDataArgs['dto'];
};

export function getUpdateChallengeInstruction<
  TAccountChallengeTemplateAccount extends string,
  TAccountChallengeAccount extends string,
  TAccountSender extends string,
  TProgramAddress extends Address = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
>(
  input: UpdateChallengeInput<
    TAccountChallengeTemplateAccount,
    TAccountChallengeAccount,
    TAccountSender
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateChallengeInstruction<
  TProgramAddress,
  TAccountChallengeTemplateAccount,
  TAccountChallengeAccount,
  TAccountSender
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_CHALLENGES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeTemplateAccount: {
      value: input.challengeTemplateAccount ?? null,
      isWritable: false,
    },
    challengeAccount: {
      value: input.challengeAccount ?? null,
      isWritable: true,
    },
    sender: { value: input.sender ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.challengeTemplateAccount),
      getAccountMeta(accounts.challengeAccount),
      getAccountMeta(accounts.sender),
    ],
    data: getUpdateChallengeInstructionDataEncoder().encode(
      args as UpdateChallengeInstructionDataArgs
    ),
    programAddress,
  } as UpdateChallengeInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountChallengeAccount,
    TAccountSender
  >);
}

export type ParsedUpdateChallengeInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challengeTemplateAccount: TAccountMetas[0];
    challengeAccount: TAccountMetas[1];
    sender: TAccountMetas[2];
  };
  data: UpdateChallengeInstructionData;
};

export function parseUpdateChallengeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateChallengeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challengeTemplateAccount: getNextAccount(),
      challengeAccount: getNextAccount(),
      sender: getNextAccount(),
    },
    data: getUpdateChallengeInstructionDataDecoder().decode(instruction.data),
  };
}
