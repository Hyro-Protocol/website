/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { POLICY_CHALLENGES_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getChallengeTemplateUpdateInsertDtoDecoder,
  getChallengeTemplateUpdateInsertDtoEncoder,
  type ChallengeTemplateUpdateInsertDto,
  type ChallengeTemplateUpdateInsertDtoArgs,
} from '../types';

export const CREATE_CHALLENGE_TEMPLATE_DISCRIMINATOR = new Uint8Array([
  143, 77, 148, 223, 18, 207, 212, 41,
]);

export function getCreateChallengeTemplateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_CHALLENGE_TEMPLATE_DISCRIMINATOR
  );
}

export type CreateChallengeTemplateInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountChallengeTemplateAccount extends
    | string
    | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallengeTemplateAccount extends string
        ? WritableAccount<TAccountChallengeTemplateAccount>
        : TAccountChallengeTemplateAccount,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateChallengeTemplateInstructionData = {
  discriminator: ReadonlyUint8Array;
  stageId: bigint;
  dto: ChallengeTemplateUpdateInsertDto;
};

export type CreateChallengeTemplateInstructionDataArgs = {
  stageId: number | bigint;
  dto: ChallengeTemplateUpdateInsertDtoArgs;
};

export function getCreateChallengeTemplateInstructionDataEncoder(): FixedSizeEncoder<CreateChallengeTemplateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['stageId', getU64Encoder()],
      ['dto', getChallengeTemplateUpdateInsertDtoEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_CHALLENGE_TEMPLATE_DISCRIMINATOR,
    })
  );
}

export function getCreateChallengeTemplateInstructionDataDecoder(): FixedSizeDecoder<CreateChallengeTemplateInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['stageId', getU64Decoder()],
    ['dto', getChallengeTemplateUpdateInsertDtoDecoder()],
  ]);
}

export function getCreateChallengeTemplateInstructionDataCodec(): FixedSizeCodec<
  CreateChallengeTemplateInstructionDataArgs,
  CreateChallengeTemplateInstructionData
> {
  return combineCodec(
    getCreateChallengeTemplateInstructionDataEncoder(),
    getCreateChallengeTemplateInstructionDataDecoder()
  );
}

export type CreateChallengeTemplateAsyncInput<
  TAccountChallengeTemplateAccount extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  challengeTemplateAccount?: Address<TAccountChallengeTemplateAccount>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  stageId: CreateChallengeTemplateInstructionDataArgs['stageId'];
  dto: CreateChallengeTemplateInstructionDataArgs['dto'];
};

export async function getCreateChallengeTemplateInstructionAsync<
  TAccountChallengeTemplateAccount extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
>(
  input: CreateChallengeTemplateAsyncInput<
    TAccountChallengeTemplateAccount,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateChallengeTemplateInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountSigner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_CHALLENGES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeTemplateAccount: {
      value: input.challengeTemplateAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.challengeTemplateAccount.value) {
    accounts.challengeTemplateAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getU64Encoder().encode(expectSome(args.stageId))],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.challengeTemplateAccount),
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateChallengeTemplateInstructionDataEncoder().encode(
      args as CreateChallengeTemplateInstructionDataArgs
    ),
    programAddress,
  } as CreateChallengeTemplateInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type CreateChallengeTemplateInput<
  TAccountChallengeTemplateAccount extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  challengeTemplateAccount: Address<TAccountChallengeTemplateAccount>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  stageId: CreateChallengeTemplateInstructionDataArgs['stageId'];
  dto: CreateChallengeTemplateInstructionDataArgs['dto'];
};

export function getCreateChallengeTemplateInstruction<
  TAccountChallengeTemplateAccount extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
>(
  input: CreateChallengeTemplateInput<
    TAccountChallengeTemplateAccount,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateChallengeTemplateInstruction<
  TProgramAddress,
  TAccountChallengeTemplateAccount,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? POLICY_CHALLENGES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeTemplateAccount: {
      value: input.challengeTemplateAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.challengeTemplateAccount),
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateChallengeTemplateInstructionDataEncoder().encode(
      args as CreateChallengeTemplateInstructionDataArgs
    ),
    programAddress,
  } as CreateChallengeTemplateInstruction<
    TProgramAddress,
    TAccountChallengeTemplateAccount,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedCreateChallengeTemplateInstruction<
  TProgram extends string = typeof POLICY_CHALLENGES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challengeTemplateAccount: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateChallengeTemplateInstructionData;
};

export function parseCreateChallengeTemplateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateChallengeTemplateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challengeTemplateAccount: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateChallengeTemplateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
