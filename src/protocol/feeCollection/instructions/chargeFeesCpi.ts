/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { FEE_COLLECTION_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getFeeOperationDecoder,
  getFeeOperationEncoder,
  type FeeOperation,
  type FeeOperationArgs,
} from '../types';

export const CHARGE_FEES_CPI_DISCRIMINATOR = new Uint8Array([
  27, 58, 69, 9, 245, 105, 154, 0,
]);

export function getChargeFeesCpiDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHARGE_FEES_CPI_DISCRIMINATOR
  );
}

export type ChargeFeesCpiInstruction<
  TProgram extends string = typeof FEE_COLLECTION_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultFees extends string | AccountMeta<string> = string,
  TAccountFeeCalcProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultFees extends string
        ? WritableAccount<TAccountVaultFees>
        : TAccountVaultFees,
      TAccountFeeCalcProgram extends string
        ? ReadonlyAccount<TAccountFeeCalcProgram>
        : TAccountFeeCalcProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ChargeFeesCpiInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** The operation triggering fee calculation */
  operation: FeeOperation;
  /** Amount involved in the operation (e.g., deposit amount) */
  amount: bigint;
  /** Current vault total balance (signed to support debt states) */
  totalBalance: bigint;
  /** Current timestamp */
  timestamp: bigint;
  /** Last fee calculation timestamp (for time-based fees) */
  lastFeeTimestamp: bigint;
  /** High water mark (for performance fees) */
  highWaterMark: bigint;
};

export type ChargeFeesCpiInstructionDataArgs = {
  /** The operation triggering fee calculation */
  operation: FeeOperationArgs;
  /** Amount involved in the operation (e.g., deposit amount) */
  amount: number | bigint;
  /** Current vault total balance (signed to support debt states) */
  totalBalance: number | bigint;
  /** Current timestamp */
  timestamp: number | bigint;
  /** Last fee calculation timestamp (for time-based fees) */
  lastFeeTimestamp: number | bigint;
  /** High water mark (for performance fees) */
  highWaterMark: number | bigint;
};

export function getChargeFeesCpiInstructionDataEncoder(): FixedSizeEncoder<ChargeFeesCpiInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['operation', getFeeOperationEncoder()],
      ['amount', getU64Encoder()],
      ['totalBalance', getI64Encoder()],
      ['timestamp', getI64Encoder()],
      ['lastFeeTimestamp', getI64Encoder()],
      ['highWaterMark', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CHARGE_FEES_CPI_DISCRIMINATOR })
  );
}

export function getChargeFeesCpiInstructionDataDecoder(): FixedSizeDecoder<ChargeFeesCpiInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['operation', getFeeOperationDecoder()],
    ['amount', getU64Decoder()],
    ['totalBalance', getI64Decoder()],
    ['timestamp', getI64Decoder()],
    ['lastFeeTimestamp', getI64Decoder()],
    ['highWaterMark', getU64Decoder()],
  ]);
}

export function getChargeFeesCpiInstructionDataCodec(): FixedSizeCodec<
  ChargeFeesCpiInstructionDataArgs,
  ChargeFeesCpiInstructionData
> {
  return combineCodec(
    getChargeFeesCpiInstructionDataEncoder(),
    getChargeFeesCpiInstructionDataDecoder()
  );
}

export type ChargeFeesCpiAsyncInput<
  TAccountVault extends string = string,
  TAccountVaultFees extends string = string,
  TAccountFeeCalcProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultFees?: Address<TAccountVaultFees>;
  feeCalcProgram: Address<TAccountFeeCalcProgram>;
  operation: ChargeFeesCpiInstructionDataArgs['operation'];
  amount: ChargeFeesCpiInstructionDataArgs['amount'];
  totalBalance: ChargeFeesCpiInstructionDataArgs['totalBalance'];
  timestamp: ChargeFeesCpiInstructionDataArgs['timestamp'];
  lastFeeTimestamp: ChargeFeesCpiInstructionDataArgs['lastFeeTimestamp'];
  highWaterMark: ChargeFeesCpiInstructionDataArgs['highWaterMark'];
};

export async function getChargeFeesCpiInstructionAsync<
  TAccountVault extends string,
  TAccountVaultFees extends string,
  TAccountFeeCalcProgram extends string,
  TProgramAddress extends Address = typeof FEE_COLLECTION_PROGRAM_ADDRESS,
>(
  input: ChargeFeesCpiAsyncInput<
    TAccountVault,
    TAccountVaultFees,
    TAccountFeeCalcProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ChargeFeesCpiInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultFees,
    TAccountFeeCalcProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FEE_COLLECTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: false },
    vaultFees: { value: input.vaultFees ?? null, isWritable: true },
    feeCalcProgram: { value: input.feeCalcProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultFees.value) {
    accounts.vaultFees.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 97, 117, 108, 116, 95, 102, 101, 101, 115])
        ),
        getAddressEncoder().encode(expectAddress(accounts.vault.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultFees),
      getAccountMeta(accounts.feeCalcProgram),
    ],
    data: getChargeFeesCpiInstructionDataEncoder().encode(
      args as ChargeFeesCpiInstructionDataArgs
    ),
    programAddress,
  } as ChargeFeesCpiInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultFees,
    TAccountFeeCalcProgram
  >);
}

export type ChargeFeesCpiInput<
  TAccountVault extends string = string,
  TAccountVaultFees extends string = string,
  TAccountFeeCalcProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultFees: Address<TAccountVaultFees>;
  feeCalcProgram: Address<TAccountFeeCalcProgram>;
  operation: ChargeFeesCpiInstructionDataArgs['operation'];
  amount: ChargeFeesCpiInstructionDataArgs['amount'];
  totalBalance: ChargeFeesCpiInstructionDataArgs['totalBalance'];
  timestamp: ChargeFeesCpiInstructionDataArgs['timestamp'];
  lastFeeTimestamp: ChargeFeesCpiInstructionDataArgs['lastFeeTimestamp'];
  highWaterMark: ChargeFeesCpiInstructionDataArgs['highWaterMark'];
};

export function getChargeFeesCpiInstruction<
  TAccountVault extends string,
  TAccountVaultFees extends string,
  TAccountFeeCalcProgram extends string,
  TProgramAddress extends Address = typeof FEE_COLLECTION_PROGRAM_ADDRESS,
>(
  input: ChargeFeesCpiInput<
    TAccountVault,
    TAccountVaultFees,
    TAccountFeeCalcProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ChargeFeesCpiInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountVaultFees,
  TAccountFeeCalcProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FEE_COLLECTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: false },
    vaultFees: { value: input.vaultFees ?? null, isWritable: true },
    feeCalcProgram: { value: input.feeCalcProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultFees),
      getAccountMeta(accounts.feeCalcProgram),
    ],
    data: getChargeFeesCpiInstructionDataEncoder().encode(
      args as ChargeFeesCpiInstructionDataArgs
    ),
    programAddress,
  } as ChargeFeesCpiInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultFees,
    TAccountFeeCalcProgram
  >);
}

export type ParsedChargeFeesCpiInstruction<
  TProgram extends string = typeof FEE_COLLECTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    vaultFees: TAccountMetas[1];
    feeCalcProgram: TAccountMetas[2];
  };
  data: ChargeFeesCpiInstructionData;
};

export function parseChargeFeesCpiInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedChargeFeesCpiInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      vaultFees: getNextAccount(),
      feeCalcProgram: getNextAccount(),
    },
    data: getChargeFeesCpiInstructionDataDecoder().decode(instruction.data),
  };
}
