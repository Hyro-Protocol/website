/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getFeeOperationDecoder,
  getFeeOperationEncoder,
  type FeeOperation,
  type FeeOperationArgs,
} from '../types';

export const CALCULATE_FEES_DISCRIMINATOR = new Uint8Array([
  140, 235, 78, 9, 249, 8, 129, 101,
]);

export function getCalculateFeesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CALCULATE_FEES_DISCRIMINATOR
  );
}

export type CalculateFeesInstruction<
  TProgram extends string = typeof FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountConfigAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountConfigAccount extends string
        ? WritableAccount<TAccountConfigAccount>
        : TAccountConfigAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CalculateFeesInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** The operation triggering fee calculation */
  operation: FeeOperation;
  /** Amount involved in the operation (e.g., deposit amount) */
  amount: bigint;
  /** Current vault total balance (signed to support debt states) */
  totalBalance: bigint;
  /** Current timestamp */
  timestamp: bigint;
  /** Last fee calculation timestamp (for time-based fees) */
  lastFeeTimestamp: bigint;
  /** High water mark (for performance fees) */
  highWaterMark: bigint;
};

export type CalculateFeesInstructionDataArgs = {
  /** The operation triggering fee calculation */
  operation: FeeOperationArgs;
  /** Amount involved in the operation (e.g., deposit amount) */
  amount: number | bigint;
  /** Current vault total balance (signed to support debt states) */
  totalBalance: number | bigint;
  /** Current timestamp */
  timestamp: number | bigint;
  /** Last fee calculation timestamp (for time-based fees) */
  lastFeeTimestamp: number | bigint;
  /** High water mark (for performance fees) */
  highWaterMark: number | bigint;
};

export function getCalculateFeesInstructionDataEncoder(): FixedSizeEncoder<CalculateFeesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['operation', getFeeOperationEncoder()],
      ['amount', getU64Encoder()],
      ['totalBalance', getI64Encoder()],
      ['timestamp', getI64Encoder()],
      ['lastFeeTimestamp', getI64Encoder()],
      ['highWaterMark', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CALCULATE_FEES_DISCRIMINATOR })
  );
}

export function getCalculateFeesInstructionDataDecoder(): FixedSizeDecoder<CalculateFeesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['operation', getFeeOperationDecoder()],
    ['amount', getU64Decoder()],
    ['totalBalance', getI64Decoder()],
    ['timestamp', getI64Decoder()],
    ['lastFeeTimestamp', getI64Decoder()],
    ['highWaterMark', getU64Decoder()],
  ]);
}

export function getCalculateFeesInstructionDataCodec(): FixedSizeCodec<
  CalculateFeesInstructionDataArgs,
  CalculateFeesInstructionData
> {
  return combineCodec(
    getCalculateFeesInstructionDataEncoder(),
    getCalculateFeesInstructionDataDecoder()
  );
}

export type CalculateFeesAsyncInput<
  TAccountVault extends string = string,
  TAccountConfigAccount extends string = string,
> = {
  vault: Address<TAccountVault>;
  configAccount?: Address<TAccountConfigAccount>;
  operation: CalculateFeesInstructionDataArgs['operation'];
  amount: CalculateFeesInstructionDataArgs['amount'];
  totalBalance: CalculateFeesInstructionDataArgs['totalBalance'];
  timestamp: CalculateFeesInstructionDataArgs['timestamp'];
  lastFeeTimestamp: CalculateFeesInstructionDataArgs['lastFeeTimestamp'];
  highWaterMark: CalculateFeesInstructionDataArgs['highWaterMark'];
};

export async function getCalculateFeesInstructionAsync<
  TAccountVault extends string,
  TAccountConfigAccount extends string,
  TProgramAddress extends
    Address = typeof FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS,
>(
  input: CalculateFeesAsyncInput<TAccountVault, TAccountConfigAccount>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CalculateFeesInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountConfigAccount
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: false },
    configAccount: { value: input.configAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.configAccount.value) {
    accounts.configAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 105, 109, 101, 95, 99, 111, 110, 102, 105, 103])
        ),
        getAddressEncoder().encode(expectAddress(accounts.vault.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.configAccount),
    ],
    data: getCalculateFeesInstructionDataEncoder().encode(
      args as CalculateFeesInstructionDataArgs
    ),
    programAddress,
  } as CalculateFeesInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountConfigAccount
  >);
}

export type CalculateFeesInput<
  TAccountVault extends string = string,
  TAccountConfigAccount extends string = string,
> = {
  vault: Address<TAccountVault>;
  configAccount: Address<TAccountConfigAccount>;
  operation: CalculateFeesInstructionDataArgs['operation'];
  amount: CalculateFeesInstructionDataArgs['amount'];
  totalBalance: CalculateFeesInstructionDataArgs['totalBalance'];
  timestamp: CalculateFeesInstructionDataArgs['timestamp'];
  lastFeeTimestamp: CalculateFeesInstructionDataArgs['lastFeeTimestamp'];
  highWaterMark: CalculateFeesInstructionDataArgs['highWaterMark'];
};

export function getCalculateFeesInstruction<
  TAccountVault extends string,
  TAccountConfigAccount extends string,
  TProgramAddress extends
    Address = typeof FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS,
>(
  input: CalculateFeesInput<TAccountVault, TAccountConfigAccount>,
  config?: { programAddress?: TProgramAddress }
): CalculateFeesInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountConfigAccount
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: false },
    configAccount: { value: input.configAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.configAccount),
    ],
    data: getCalculateFeesInstructionDataEncoder().encode(
      args as CalculateFeesInstructionDataArgs
    ),
    programAddress,
  } as CalculateFeesInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountConfigAccount
  >);
}

export type ParsedCalculateFeesInstruction<
  TProgram extends string = typeof FEE_COLLECTION_TIME_BASED_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    configAccount: TAccountMetas[1];
  };
  data: CalculateFeesInstructionData;
};

export function parseCalculateFeesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCalculateFeesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vault: getNextAccount(), configAccount: getNextAccount() },
    data: getCalculateFeesInstructionDataDecoder().decode(instruction.data),
  };
}
