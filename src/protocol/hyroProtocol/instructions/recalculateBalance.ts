/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HYRO_PROTOCOL_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const RECALCULATE_BALANCE_DISCRIMINATOR = new Uint8Array([
  119, 21, 228, 154, 19, 193, 246, 8,
]);

export function getRecalculateBalanceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECALCULATE_BALANCE_DISCRIMINATOR,
  );
}

export type RecalculateBalanceInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountUnderlyingMint extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountUnderlyingMint extends string
        ? WritableAccount<TAccountUnderlyingMint>
        : TAccountUnderlyingMint,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type RecalculateBalanceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RecalculateBalanceInstructionDataArgs = {};

export function getRecalculateBalanceInstructionDataEncoder(): FixedSizeEncoder<RecalculateBalanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RECALCULATE_BALANCE_DISCRIMINATOR }),
  );
}

export function getRecalculateBalanceInstructionDataDecoder(): FixedSizeDecoder<RecalculateBalanceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRecalculateBalanceInstructionDataCodec(): FixedSizeCodec<
  RecalculateBalanceInstructionDataArgs,
  RecalculateBalanceInstructionData
> {
  return combineCodec(
    getRecalculateBalanceInstructionDataEncoder(),
    getRecalculateBalanceInstructionDataDecoder(),
  );
}

export type RecalculateBalanceAsyncInput<
  TAccountVaultTokenAccount extends string = string,
  TAccountUnderlyingMint extends string = string,
  TAccountVault extends string = string,
  TAccountSigner extends string = string,
> = {
  vaultTokenAccount?: Address<TAccountVaultTokenAccount>;
  underlyingMint: Address<TAccountUnderlyingMint>;
  vault: Address<TAccountVault>;
  signer: TransactionSigner<TAccountSigner>;
};

export async function getRecalculateBalanceInstructionAsync<
  TAccountVaultTokenAccount extends string,
  TAccountUnderlyingMint extends string,
  TAccountVault extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: RecalculateBalanceAsyncInput<
    TAccountVaultTokenAccount,
    TAccountUnderlyingMint,
    TAccountVault,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecalculateBalanceInstruction<
    TProgramAddress,
    TAccountVaultTokenAccount,
    TAccountUnderlyingMint,
    TAccountVault,
    TAccountSigner
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    underlyingMint: { value: input.underlyingMint ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vaultTokenAccount.value) {
    accounts.vaultTokenAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            118, 97, 117, 108, 116, 95, 116, 111, 107, 101, 110, 95, 97, 99, 99,
            111, 117, 110, 116,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.vault.value)),
        getAddressEncoder().encode(
          expectAddress(accounts.underlyingMint.value),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.underlyingMint),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.signer),
    ],
    data: getRecalculateBalanceInstructionDataEncoder().encode({}),
    programAddress,
  } as RecalculateBalanceInstruction<
    TProgramAddress,
    TAccountVaultTokenAccount,
    TAccountUnderlyingMint,
    TAccountVault,
    TAccountSigner
  >);
}

export type RecalculateBalanceInput<
  TAccountVaultTokenAccount extends string = string,
  TAccountUnderlyingMint extends string = string,
  TAccountVault extends string = string,
  TAccountSigner extends string = string,
> = {
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  underlyingMint: Address<TAccountUnderlyingMint>;
  vault: Address<TAccountVault>;
  signer: TransactionSigner<TAccountSigner>;
};

export function getRecalculateBalanceInstruction<
  TAccountVaultTokenAccount extends string,
  TAccountUnderlyingMint extends string,
  TAccountVault extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: RecalculateBalanceInput<
    TAccountVaultTokenAccount,
    TAccountUnderlyingMint,
    TAccountVault,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress },
): RecalculateBalanceInstruction<
  TProgramAddress,
  TAccountVaultTokenAccount,
  TAccountUnderlyingMint,
  TAccountVault,
  TAccountSigner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    underlyingMint: { value: input.underlyingMint ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.underlyingMint),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.signer),
    ],
    data: getRecalculateBalanceInstructionDataEncoder().encode({}),
    programAddress,
  } as RecalculateBalanceInstruction<
    TProgramAddress,
    TAccountVaultTokenAccount,
    TAccountUnderlyingMint,
    TAccountVault,
    TAccountSigner
  >);
}

export type ParsedRecalculateBalanceInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultTokenAccount: TAccountMetas[0];
    underlyingMint: TAccountMetas[1];
    vault: TAccountMetas[2];
    signer: TAccountMetas[3];
  };
  data: RecalculateBalanceInstructionData;
};

export function parseRecalculateBalanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecalculateBalanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultTokenAccount: getNextAccount(),
      underlyingMint: getNextAccount(),
      vault: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getRecalculateBalanceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
