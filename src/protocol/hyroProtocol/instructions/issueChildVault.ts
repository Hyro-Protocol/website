/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HYRO_PROTOCOL_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getManagerFeeStructureDecoder,
  getManagerFeeStructureEncoder,
  type ManagerFeeStructure,
  type ManagerFeeStructureArgs,
} from "../types";

export const ISSUE_CHILD_VAULT_DISCRIMINATOR = new Uint8Array([
  77, 93, 95, 1, 227, 206, 201, 173,
]);

export function getIssueChildVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ISSUE_CHILD_VAULT_DISCRIMINATOR,
  );
}

export type IssueChildVaultInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountParentVault extends string | AccountMeta<string> = string,
  TAccountChildVault extends string | AccountMeta<string> = string,
  TAccountChildAuthority extends string | AccountMeta<string> = string,
  TAccountManagerRegistry extends string | AccountMeta<string> = string,
  TAccountManagerProfile extends string | AccountMeta<string> = string,
  TAccountChildPolicy extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountParentVault extends string
        ? WritableAccount<TAccountParentVault>
        : TAccountParentVault,
      TAccountChildVault extends string
        ? WritableAccount<TAccountChildVault>
        : TAccountChildVault,
      TAccountChildAuthority extends string
        ? ReadonlyAccount<TAccountChildAuthority>
        : TAccountChildAuthority,
      TAccountManagerRegistry extends string
        ? WritableAccount<TAccountManagerRegistry>
        : TAccountManagerRegistry,
      TAccountManagerProfile extends string
        ? WritableAccount<TAccountManagerProfile>
        : TAccountManagerProfile,
      TAccountChildPolicy extends string
        ? ReadonlyAccount<TAccountChildPolicy>
        : TAccountChildPolicy,
      TAccountManager extends string
        ? ReadonlyAccount<TAccountManager>
        : TAccountManager,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type IssueChildVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  seed: string;
  allocation: bigint;
  managerFees: ManagerFeeStructure;
};

export type IssueChildVaultInstructionDataArgs = {
  seed: string;
  allocation: number | bigint;
  managerFees: ManagerFeeStructureArgs;
};

export function getIssueChildVaultInstructionDataEncoder(): Encoder<IssueChildVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["seed", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["allocation", getU64Encoder()],
      ["managerFees", getManagerFeeStructureEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ISSUE_CHILD_VAULT_DISCRIMINATOR }),
  );
}

export function getIssueChildVaultInstructionDataDecoder(): Decoder<IssueChildVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["seed", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["allocation", getU64Decoder()],
    ["managerFees", getManagerFeeStructureDecoder()],
  ]);
}

export function getIssueChildVaultInstructionDataCodec(): Codec<
  IssueChildVaultInstructionDataArgs,
  IssueChildVaultInstructionData
> {
  return combineCodec(
    getIssueChildVaultInstructionDataEncoder(),
    getIssueChildVaultInstructionDataDecoder(),
  );
}

export type IssueChildVaultAsyncInput<
  TAccountParentVault extends string = string,
  TAccountChildVault extends string = string,
  TAccountChildAuthority extends string = string,
  TAccountManagerRegistry extends string = string,
  TAccountManagerProfile extends string = string,
  TAccountChildPolicy extends string = string,
  TAccountManager extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  parentVault: Address<TAccountParentVault>;
  childVault?: Address<TAccountChildVault>;
  childAuthority?: Address<TAccountChildAuthority>;
  managerRegistry: Address<TAccountManagerRegistry>;
  managerProfile: Address<TAccountManagerProfile>;
  childPolicy: Address<TAccountChildPolicy>;
  manager: Address<TAccountManager>;
  admin: TransactionSigner<TAccountAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  seed: IssueChildVaultInstructionDataArgs["seed"];
  allocation: IssueChildVaultInstructionDataArgs["allocation"];
  managerFees: IssueChildVaultInstructionDataArgs["managerFees"];
};

export async function getIssueChildVaultInstructionAsync<
  TAccountParentVault extends string,
  TAccountChildVault extends string,
  TAccountChildAuthority extends string,
  TAccountManagerRegistry extends string,
  TAccountManagerProfile extends string,
  TAccountChildPolicy extends string,
  TAccountManager extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: IssueChildVaultAsyncInput<
    TAccountParentVault,
    TAccountChildVault,
    TAccountChildAuthority,
    TAccountManagerRegistry,
    TAccountManagerProfile,
    TAccountChildPolicy,
    TAccountManager,
    TAccountAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  IssueChildVaultInstruction<
    TProgramAddress,
    TAccountParentVault,
    TAccountChildVault,
    TAccountChildAuthority,
    TAccountManagerRegistry,
    TAccountManagerProfile,
    TAccountChildPolicy,
    TAccountManager,
    TAccountAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    parentVault: { value: input.parentVault ?? null, isWritable: true },
    childVault: { value: input.childVault ?? null, isWritable: true },
    childAuthority: { value: input.childAuthority ?? null, isWritable: false },
    managerRegistry: { value: input.managerRegistry ?? null, isWritable: true },
    managerProfile: { value: input.managerProfile ?? null, isWritable: true },
    childPolicy: { value: input.childPolicy ?? null, isWritable: false },
    manager: { value: input.manager ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.childVault.value) {
    accounts.childVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getAddressEncoder().encode(expectAddress(accounts.parentVault.value)),
        getUtf8Encoder().encode(expectSome(args.seed)),
        getU64Encoder().encode(expectSome(args.allocation)),
        getAddressEncoder().encode(expectAddress(accounts.manager.value)),
      ],
    });
  }
  if (!accounts.childAuthority.value) {
    accounts.childAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getAddressEncoder().encode(expectAddress(accounts.childVault.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.parentVault),
      getAccountMeta(accounts.childVault),
      getAccountMeta(accounts.childAuthority),
      getAccountMeta(accounts.managerRegistry),
      getAccountMeta(accounts.managerProfile),
      getAccountMeta(accounts.childPolicy),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getIssueChildVaultInstructionDataEncoder().encode(
      args as IssueChildVaultInstructionDataArgs,
    ),
    programAddress,
  } as IssueChildVaultInstruction<
    TProgramAddress,
    TAccountParentVault,
    TAccountChildVault,
    TAccountChildAuthority,
    TAccountManagerRegistry,
    TAccountManagerProfile,
    TAccountChildPolicy,
    TAccountManager,
    TAccountAdmin,
    TAccountSystemProgram
  >);
}

export type IssueChildVaultInput<
  TAccountParentVault extends string = string,
  TAccountChildVault extends string = string,
  TAccountChildAuthority extends string = string,
  TAccountManagerRegistry extends string = string,
  TAccountManagerProfile extends string = string,
  TAccountChildPolicy extends string = string,
  TAccountManager extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  parentVault: Address<TAccountParentVault>;
  childVault: Address<TAccountChildVault>;
  childAuthority: Address<TAccountChildAuthority>;
  managerRegistry: Address<TAccountManagerRegistry>;
  managerProfile: Address<TAccountManagerProfile>;
  childPolicy: Address<TAccountChildPolicy>;
  manager: Address<TAccountManager>;
  admin: TransactionSigner<TAccountAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  seed: IssueChildVaultInstructionDataArgs["seed"];
  allocation: IssueChildVaultInstructionDataArgs["allocation"];
  managerFees: IssueChildVaultInstructionDataArgs["managerFees"];
};

export function getIssueChildVaultInstruction<
  TAccountParentVault extends string,
  TAccountChildVault extends string,
  TAccountChildAuthority extends string,
  TAccountManagerRegistry extends string,
  TAccountManagerProfile extends string,
  TAccountChildPolicy extends string,
  TAccountManager extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: IssueChildVaultInput<
    TAccountParentVault,
    TAccountChildVault,
    TAccountChildAuthority,
    TAccountManagerRegistry,
    TAccountManagerProfile,
    TAccountChildPolicy,
    TAccountManager,
    TAccountAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): IssueChildVaultInstruction<
  TProgramAddress,
  TAccountParentVault,
  TAccountChildVault,
  TAccountChildAuthority,
  TAccountManagerRegistry,
  TAccountManagerProfile,
  TAccountChildPolicy,
  TAccountManager,
  TAccountAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    parentVault: { value: input.parentVault ?? null, isWritable: true },
    childVault: { value: input.childVault ?? null, isWritable: true },
    childAuthority: { value: input.childAuthority ?? null, isWritable: false },
    managerRegistry: { value: input.managerRegistry ?? null, isWritable: true },
    managerProfile: { value: input.managerProfile ?? null, isWritable: true },
    childPolicy: { value: input.childPolicy ?? null, isWritable: false },
    manager: { value: input.manager ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.parentVault),
      getAccountMeta(accounts.childVault),
      getAccountMeta(accounts.childAuthority),
      getAccountMeta(accounts.managerRegistry),
      getAccountMeta(accounts.managerProfile),
      getAccountMeta(accounts.childPolicy),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getIssueChildVaultInstructionDataEncoder().encode(
      args as IssueChildVaultInstructionDataArgs,
    ),
    programAddress,
  } as IssueChildVaultInstruction<
    TProgramAddress,
    TAccountParentVault,
    TAccountChildVault,
    TAccountChildAuthority,
    TAccountManagerRegistry,
    TAccountManagerProfile,
    TAccountChildPolicy,
    TAccountManager,
    TAccountAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedIssueChildVaultInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    parentVault: TAccountMetas[0];
    childVault: TAccountMetas[1];
    childAuthority: TAccountMetas[2];
    managerRegistry: TAccountMetas[3];
    managerProfile: TAccountMetas[4];
    childPolicy: TAccountMetas[5];
    manager: TAccountMetas[6];
    admin: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: IssueChildVaultInstructionData;
};

export function parseIssueChildVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedIssueChildVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      parentVault: getNextAccount(),
      childVault: getNextAccount(),
      childAuthority: getNextAccount(),
      managerRegistry: getNextAccount(),
      managerProfile: getNextAccount(),
      childPolicy: getNextAccount(),
      manager: getNextAccount(),
      admin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getIssueChildVaultInstructionDataDecoder().decode(instruction.data),
  };
}
