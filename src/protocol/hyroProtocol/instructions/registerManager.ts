/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HYRO_PROTOCOL_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRiskRatingDecoder,
  getRiskRatingEncoder,
  type RiskRating,
  type RiskRatingArgs,
} from "../types";

export const REGISTER_MANAGER_DISCRIMINATOR = new Uint8Array([
  104, 14, 206, 198, 57, 194, 90, 109,
]);

export function getRegisterManagerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_MANAGER_DISCRIMINATOR,
  );
}

export type RegisterManagerInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountManagerProfile extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountManager extends string
        ? ReadonlyAccount<TAccountManager>
        : TAccountManager,
      TAccountManagerProfile extends string
        ? WritableAccount<TAccountManagerProfile>
        : TAccountManagerProfile,
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterManagerInstructionData = {
  discriminator: ReadonlyUint8Array;
  riskRating: RiskRating;
};

export type RegisterManagerInstructionDataArgs = { riskRating: RiskRatingArgs };

export function getRegisterManagerInstructionDataEncoder(): FixedSizeEncoder<RegisterManagerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["riskRating", getRiskRatingEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_MANAGER_DISCRIMINATOR }),
  );
}

export function getRegisterManagerInstructionDataDecoder(): FixedSizeDecoder<RegisterManagerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["riskRating", getRiskRatingDecoder()],
  ]);
}

export function getRegisterManagerInstructionDataCodec(): FixedSizeCodec<
  RegisterManagerInstructionDataArgs,
  RegisterManagerInstructionData
> {
  return combineCodec(
    getRegisterManagerInstructionDataEncoder(),
    getRegisterManagerInstructionDataDecoder(),
  );
}

export type RegisterManagerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountManager extends string = string,
  TAccountManagerProfile extends string = string,
  TAccountRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  manager: Address<TAccountManager>;
  managerProfile?: Address<TAccountManagerProfile>;
  registry: Address<TAccountRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  riskRating: RegisterManagerInstructionDataArgs["riskRating"];
};

export async function getRegisterManagerInstructionAsync<
  TAccountAdmin extends string,
  TAccountManager extends string,
  TAccountManagerProfile extends string,
  TAccountRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: RegisterManagerAsyncInput<
    TAccountAdmin,
    TAccountManager,
    TAccountManagerProfile,
    TAccountRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterManagerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountManager,
    TAccountManagerProfile,
    TAccountRegistry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    managerProfile: { value: input.managerProfile ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.managerProfile.value) {
    accounts.managerProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            109, 97, 110, 97, 103, 101, 114, 95, 112, 114, 111, 102, 105, 108,
            101,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.manager.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.managerProfile),
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterManagerInstructionDataEncoder().encode(
      args as RegisterManagerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterManagerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountManager,
    TAccountManagerProfile,
    TAccountRegistry,
    TAccountSystemProgram
  >);
}

export type RegisterManagerInput<
  TAccountAdmin extends string = string,
  TAccountManager extends string = string,
  TAccountManagerProfile extends string = string,
  TAccountRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  manager: Address<TAccountManager>;
  managerProfile: Address<TAccountManagerProfile>;
  registry: Address<TAccountRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  riskRating: RegisterManagerInstructionDataArgs["riskRating"];
};

export function getRegisterManagerInstruction<
  TAccountAdmin extends string,
  TAccountManager extends string,
  TAccountManagerProfile extends string,
  TAccountRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: RegisterManagerInput<
    TAccountAdmin,
    TAccountManager,
    TAccountManagerProfile,
    TAccountRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterManagerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountManager,
  TAccountManagerProfile,
  TAccountRegistry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    managerProfile: { value: input.managerProfile ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.managerProfile),
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterManagerInstructionDataEncoder().encode(
      args as RegisterManagerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterManagerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountManager,
    TAccountManagerProfile,
    TAccountRegistry,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterManagerInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    manager: TAccountMetas[1];
    managerProfile: TAccountMetas[2];
    registry: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterManagerInstructionData;
};

export function parseRegisterManagerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterManagerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      manager: getNextAccount(),
      managerProfile: getNextAccount(),
      registry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterManagerInstructionDataDecoder().decode(instruction.data),
  };
}
