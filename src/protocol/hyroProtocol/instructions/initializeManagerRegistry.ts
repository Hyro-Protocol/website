/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { HYRO_PROTOCOL_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INITIALIZE_MANAGER_REGISTRY_DISCRIMINATOR = new Uint8Array([
  45, 38, 8, 215, 181, 198, 110, 202,
]);

export function getInitializeManagerRegistryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_MANAGER_REGISTRY_DISCRIMINATOR
  );
}

export type InitializeManagerRegistryInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeManagerRegistryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeManagerRegistryInstructionDataArgs = {};

export function getInitializeManagerRegistryInstructionDataEncoder(): FixedSizeEncoder<InitializeManagerRegistryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_MANAGER_REGISTRY_DISCRIMINATOR,
    })
  );
}

export function getInitializeManagerRegistryInstructionDataDecoder(): FixedSizeDecoder<InitializeManagerRegistryInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeManagerRegistryInstructionDataCodec(): FixedSizeCodec<
  InitializeManagerRegistryInstructionDataArgs,
  InitializeManagerRegistryInstructionData
> {
  return combineCodec(
    getInitializeManagerRegistryInstructionDataEncoder(),
    getInitializeManagerRegistryInstructionDataDecoder()
  );
}

export type InitializeManagerRegistryAsyncInput<
  TAccountRegistry extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  registry?: Address<TAccountRegistry>;
  admin: TransactionSigner<TAccountAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeManagerRegistryInstructionAsync<
  TAccountRegistry extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: InitializeManagerRegistryAsyncInput<
    TAccountRegistry,
    TAccountAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeManagerRegistryInstruction<
    TProgramAddress,
    TAccountRegistry,
    TAccountAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registry: { value: input.registry ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            109, 97, 110, 97, 103, 101, 114, 95, 114, 101, 103, 105, 115, 116,
            114, 121,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeManagerRegistryInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeManagerRegistryInstruction<
    TProgramAddress,
    TAccountRegistry,
    TAccountAdmin,
    TAccountSystemProgram
  >);
}

export type InitializeManagerRegistryInput<
  TAccountRegistry extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  registry: Address<TAccountRegistry>;
  admin: TransactionSigner<TAccountAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeManagerRegistryInstruction<
  TAccountRegistry extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: InitializeManagerRegistryInput<
    TAccountRegistry,
    TAccountAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeManagerRegistryInstruction<
  TProgramAddress,
  TAccountRegistry,
  TAccountAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HYRO_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registry: { value: input.registry ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeManagerRegistryInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeManagerRegistryInstruction<
    TProgramAddress,
    TAccountRegistry,
    TAccountAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeManagerRegistryInstruction<
  TProgram extends string = typeof HYRO_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    registry: TAccountMetas[0];
    admin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeManagerRegistryInstructionData;
};

export function parseInitializeManagerRegistryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeManagerRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      registry: getNextAccount(),
      admin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeManagerRegistryInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
